[!warning]
注意：目前仅支持统计全球（含中国大陆）流量数据

---

# 使用方法

前往 https://console.cloud.tencent.com/ 创建一个访问密钥，必须有权限： `QcloudTEOReadOnlyaccess` 即 **边缘安全加速平台 EO 只读访问权限**

fork 该仓库，部署到 EdgeOne Pages，传入环境变量 `SECRET_ID` 和 `SECRET_KEY` 即可